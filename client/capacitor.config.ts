import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Sistema de Gestion de Inicidencias',
  webDir: 'dist'
};

export default config;
